apiVersion: v1
kind: Pod
metadata:
  name: naughty-root
  namespace: audit-zone
spec:
  securityContext:
    runAsUser: 0
  containers:
    - name: nginx
      image: nginx:latest
      command: ["nginx", "-g", "daemon off;"]